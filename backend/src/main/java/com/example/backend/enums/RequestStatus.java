package com.example.backend.enums;

/**
 * RequestStatus: Represents the state of a Secret Change Request 
 * inside the enterprise approval workflow.
 */
public enum RequestStatus {
    PENDING,
    APPROVED,
    REJECTED
}
